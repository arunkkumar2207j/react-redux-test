import React from 'react'
import './styles.scss'

const Header = (props) => {
    return (
        <header className="headerComponent">
            <div className="wrap">
                SimpleTuts
            </div>
        </header>
    )
}

export default Header;