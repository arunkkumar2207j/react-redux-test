import React from 'react'
import './styles.scss'

const Header = (props) => {
    return (
        <header>
            <div className="wrap">
                SimpleTuts
            </div>
        </header>
    )
}

export default Header;